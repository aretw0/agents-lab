# TASK-BUD-080 — Plan (2026-04-21)

## Objetivo
Instituir protocolo canônico de **scan bounded-by-default** no compact loop para evitar novo salto abrupto de contexto durante investigação/triagem.

## Contexto do incidente
- Durante investigação de warning de contexto, uma busca ampla em histórico elevou consumo para ~83%.
- Efeito: parada abrupta da execução e necessidade de compactação emergencial.

## Escopo do micro-slice atual
1. Registrar decisão e task canônicas (`DEC-BUD-033`, `TASK-BUD-080`).
2. Atualizar handoff pós-compact com trilha de prevenção.
3. Preparar próxima edição de runbook (sem varredura ampla nesta etapa).

## Protocolo proposto (draft)
- Em `context_watch.level=warn` ou superior:
  - evitar busca recursiva/global em logs de sessão;
  - consultar arquivo alvo único com `read` + `offset/limit`;
  - limitar inspeção a janelas pequenas e hipóteses explícitas.
- Em `checkpoint`:
  - somente micro-slices + checkpoint canônico curto.
- Em `compact`:
  - parar investigação, compactar, retomar via handoff.

## Próximo micro-slice
- Atualizar `docs/guides/openai-context-window-playbook.md` e `docs/guides/project-canonical-pipeline.md` com checklist operacional de scan-bounds.
